<%= form_with(model: servidor) do |form| %>
  <% if servidor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(servidor.errors.count, "error") %> prohibited this servidor from being saved:</h2>

      <ul>
        <% servidor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
